<UserControl x:Class="ParentalControlHider.ParentalControlHiderSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
            <Setter Property="Margin" Value="0,5,5,0" />
            <Setter Property="TextWrapping" Value="Wrap"></Setter>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="5,0,5,0"></Setter>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Margin" Value="2.5,2.5,2.5,2.5"></Setter>
        </Style>
        <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type RadioButton}}">
            <Setter Property="Margin" Value="0,2.5,0,2.5"></Setter>
        </Style>
    </UserControl.Resources>
    <TabControl>
        <TabItem Header="{DynamicResource LOC_ParentalControlHider_Settings_AgeRatings_Header}">
            <ListView 
                ItemsSource="{Binding AgeRatings}"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
                <ListView.View>
                    <GridView>
                        <GridViewColumn>
                            <GridViewColumnHeader>
                                <TextBlock Text="{DynamicResource LOC_ParentalControlHider_Settings_AgeRatings_IsUsed}" />
                            </GridViewColumnHeader>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsUsed}">
                                        <TextBlock Text="{Binding Name}" />
                                    </CheckBox>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn>
                            <GridViewColumnHeader>
                                <TextBlock Text="{DynamicResource LOC_ParentalControlHider_Settings_AgeRatings_MinimumAge}" />
                            </GridViewColumnHeader>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Age}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </TabItem>
        <TabItem Header="{DynamicResource LOC_ParentalControlHider_Settings_Tags_Header}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                    <ColumnDefinition Width="75"></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" Text="{DynamicResource LOC_ParentalControlHider_Settings_Tags_Allowed}"></TextBlock>
                <TextBlock Grid.Column="2" Grid.Row="0" Text="{DynamicResource LOC_ParentalControlHider_Settings_Tags_Blacklisted}"></TextBlock>
                <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding AllowedTags}" SelectedItem="{Binding SelectedAllowedTag}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"></TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Grid Column="1" Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="50"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="1" Content="&gt;" Command="{Binding BlacklistTagCommand}"></Button>
                    <Button Grid.Row="3" Content="&lt;" Command="{Binding AllowTagCommand}"></Button>
                </Grid>
                <ListBox Grid.Column="2" Grid.Row="1" ItemsSource="{Binding BlacklistedTags}" SelectedItem="{Binding SelectedBlacklistedTag}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"></TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Grid Grid.Column="0" Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{DynamicResource LOC_ParentalControlHider_Settings_Tags_Filter}"></TextBlock>
                    <TextBox Grid.Column="1" Text="{Binding AllowedTagsFilter}"></TextBox>
                </Grid>
                <Grid Grid.Column="2" Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{DynamicResource LOC_ParentalControlHider_Settings_Tags_Filter}"></TextBlock>
                    <TextBox Grid.Column="1" Text="{Binding BlacklistedTagsFilter}"></TextBox>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>